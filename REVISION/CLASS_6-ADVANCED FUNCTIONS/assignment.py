"""
Create a function called 'counter_factory' that:
1. Creates a counter starting at 0
2. Returns a nested function that:
   - Increments the counter
   - Returns the new value
3. Each counter should be independent

Example:
counter1 = counter_factory()
counter2 = counter_factory()
counter1() # Returns 1
counter1() # Returns 2
counter2() # Returns 1
"""

"""
1. Create a list of dictionaries containing student records:
   [{'name': 'Alice', 'grade': 85}, {'name': 'Bob', 'grade': 92}, ...]

2. Using lambda functions and list operations:
   - Sort the list by grades (descending)
   - Filter students with grades above 90
   - Create a new list with just the names of top students
"""

"""
Create a function called 'analyze_numbers' that:
1. Takes a list of numbers
2. Returns a tuple containing:
   - List of numbers sorted in ascending order
   - Average value
   - Median value
   - List of duplicates (if any)

Example:
analyze_numbers([4, 2, 6, 2, 8, 4, 1])
Returns: ([1, 2, 2, 4, 4, 6, 8], 3.857, 4, [2, 4])
"""